/**
 * Write JS function "emitSanction" to check wether a vehicle has exceeded allowed speed limit or not
    - if speed exceeds, then return uppercase string
    - otherwise, return lower case string
 * input: string (sentence) 
 * output: string
 */
